import * as actionType from '../../../constants/actions';
import initialState from '../../stores/initialState'

const bookshelvesReducer = (state = initialState.bookshelves, action) => {
    return state;
};

export default bookshelvesReducer;
